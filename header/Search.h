#include <stdio.h>
#include <stdlib.h>

int linearSearch(int *, int, int);

int binarySearch(int *, int, int);

void floorCeiling(int *, int, int);

int find(int *, int);

int oddOne(int *, int, int);
